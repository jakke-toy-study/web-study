/**
 * 3D 좌표 클래스
 */
export class Point3d {
    /**
     * 3D 좌표 생성자
     * @param {number} x 
     * @param {number} y 
     * @param {number} z
     */
    constructor(x, y, z) {
        this.x = x;
        this.y = y;
        this.z = z;
    }
}